# Система Управления Дебиторской Задолженностью

REST API для управления и анализа дебиторской задолженности с поддержкой импорта данных из 1С:Бухгалтерия.

## 🚀 Быстрый старт

### Требования

- Node.js >= 18.x
- MongoDB >= 5.x
- npm или yarn

### Установка

```bash
# 1. Клонировать репозиторий
git clone <repository-url>
cd ShnurAA_214371_RIOPK_Server

# 2. Установить зависимости
npm install

# 3. Настроить переменные окружения
cp .env.example .env
# Отредактируйте .env файл, указав настройки MongoDB

# 4. Запустить в режиме разработки
npm run dev
```

Сервер запустится на `http://localhost:3001`

## 📚 Документация

### Swagger API Документация

После запуска сервера доступна по адресу:
```
http://localhost:3001/api-docs
```

### Дополнительная документация

- **[MVP_RECEIVABLES_MANAGEMENT.md](./MVP_RECEIVABLES_MANAGEMENT.md)** - Описание реализованного функционала MVP
- **[AGING_BUCKETS_API_GUIDE.md](./AGING_BUCKETS_API_GUIDE.md)** - Руководство по фильтрации по категориям старения
- **[TECHNICAL_UPLOAD_1C_GUIDE.md](./TECHNICAL_UPLOAD_1C_GUIDE.md)** - Техническое описание загрузки из 1С
- **[POSTMAN_UPLOAD_INSTRUCTIONS.md](./POSTMAN_UPLOAD_INSTRUCTIONS.md)** - Инструкции по загрузке через Postman

## 🔑 Основные функции

### 1. Аутентификация
- Регистрация пользователей (`POST /api/v1/auth/register`)
- Вход в систему (`POST /api/v1/auth/login`)
- Получение данных текущего пользователя (`GET /api/v1/auth/me`)

### 2. Импорт данных
- Загрузка CSV файлов из 1С (`POST /api/v1/data-uploads/1c-invoices`)
- Автоматическое создание клиентов
- Обновление существующих счетов

### 3. Отчеты и аналитика
- **Dashboard** (`GET /api/v1/reports/dashboard/summary`) - сводная информация по ДЗ
- **Топ должников** (`GET /api/v1/reports/top-debtors`) - список крупнейших должников
- **Список счетов** (`GET /api/v1/reports/invoices`) - фильтрация и поиск счетов
- **Клиенты с просрочкой** (`GET /api/v1/reports/customers-overdue`) - анализ по aging buckets
- **Применение оплаты** (`POST /api/v1/reports/invoices/apply-payment`) - частичная/полная оплата

### 4. Управление клиентами
- Список клиентов (`GET /api/v1/customers`)
- Детали клиента (`GET /api/v1/customers/:id`)

## 🏗️ Архитектура

Проект использует Clean Architecture:

```
src/
├── domain/           # Доменные сущности и интерфейсы
├── application/      # Use-cases и DTOs
└── infrastructure/   # Реализация (Express, MongoDB, DI)
```

## 🛠️ Технологии

- **TypeScript** - типизированный JavaScript
- **Express.js** - веб-фреймворк
- **MongoDB + Mongoose** - база данных
- **tsyringe** - dependency injection
- **Swagger** - API документация
- **JWT** - аутентификация
- **Jest** - тестирование

## 📝 Скрипты

```bash
npm run dev          # Запуск в режиме разработки
npm run build        # Сборка проекта
npm start            # Запуск production версии
npm test             # Запуск тестов
npm run lint         # Проверка кода линтером
npm run format       # Форматирование кода
```

## 🔒 Безопасность

- JWT токены для аутентификации
- Роли пользователей (ADMIN, ANALYST, MANAGER)
- Валидация входных данных
- Защита от SQL injection (MongoDB)

## 📊 Примеры использования

### Регистрация и вход

```bash
# Регистрация
curl -X POST http://localhost:3001/api/v1/auth/register \
  -H "Content-Type: application/json" \
  -d '{"email": "admin@test.com", "password": "password123", "name": "Admin"}'

# Вход
curl -X POST http://localhost:3001/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email": "admin@test.com", "password": "password123"}'
```

### Загрузка CSV из 1С

```bash
curl -X POST http://localhost:3001/api/v1/data-uploads/1c-invoices \
  -H "Authorization: Bearer <token>" \
  -F "file=@выгрузка1c.csv"
```

### Получение Dashboard

```bash
curl http://localhost:3001/api/v1/reports/dashboard/summary \
  -H "Authorization: Bearer <token>"
```

## 🧪 Тестирование

```bash
# Запуск всех тестов
npm test

# Запуск тестов в watch режиме
npm run test:watch
```

## 📦 Структура проекта

```
├── src/                    # Исходный код
│   ├── application/       # Use-cases и DTOs
│   ├── domain/            # Доменные сущности
│   └── infrastructure/    # Реализация
├── tests/                 # Тесты
├── dist/                  # Скомпилированный код
└── coverage/              # Покрытие тестами
```

## 🤝 Вклад в проект

1. Создайте ветку для новой функции
2. Внесите изменения
3. Напишите тесты
4. Создайте Pull Request

## 📄 Лицензия

ISC

## 📞 Поддержка

При возникновении вопросов или обнаружении багов, пожалуйста, создайте issue в репозитории.

---

**Статус:** MVP готов к тестированию 🚀

