# 📊 MVP Системы Управления Дебиторской Задолженностью

## ✅ Реализованные функции

### 1. **Расширенная доменная модель** ✓

#### Invoice Entity
Добавлены поля специфики бизнеса:
- `serviceStartDate` / `serviceEndDate` - период оказания услуги
- `serviceType` - тип услуги (ПКТ, ККТ, торговые автоматы)
- `manager` - ответственный менеджер
- `debtWorkStatus` - статус работы с долгом (Прозвон, Претензия, Суд и т.д.)
- `paymentTermDays` - срок оплаты в днях
- `actualPaymentDate` - дата фактической оплаты
- `lastContactDate` - дата последнего контакта
- `contactResult` - результат контакта
- `notes` - примечания

#### Методы Invoice Entity
- `getDaysOverdue()` - количество дней просрочки
- `calculateDebtWorkStatus()` - автоматический расчет статуса долга
- `applyPayment()` - применение частичной оплаты

---

### 2. **Импорт CSV из 1С** ✓

#### Endpoint
```
POST /api/v1/data-uploads/1c-invoices
Content-Type: multipart/form-data
Authorization: Bearer <token>

file: выгрузка1c.csv
```

#### Формат CSV (выгрузка1c.csv)
```csv
Дата_начала_услуги,Дата_окончания_услуги,Номер_акта,Контрагент,ИНН,Договор,Тип_услуги,Сумма_к_оплате,Срок_оплаты_дней,Дата_планируемой_оплаты,Сумма_оплачено,Дата_фактической_оплаты,Остаток_задолженности,Менеджер,Примечание
2024-07-01,2024-07-31,АКТ-2024-0234,ООО "АльфаТорг",123456789,Д-2023-045,Сопровождение ПКТ,15000,30,2024-08-30,15000,2024-08-25,0,Иванова М.П.,Оплачено в срок
```

#### Функционал
- ✅ Автоматическое создание контрагентов по ИНН
- ✅ Автоматический маппинг типов услуг
- ✅ Расчет статуса работы с долгом по просрочке
- ✅ Обновление существующих счетов при повторной загрузке
- ✅ Детальный отчет об ошибках

---

### 3. **Расширенный Dashboard** ✓

#### Endpoint
```
GET /api/v1/reports/dashboard/summary
Authorization: Bearer <token>
```

#### Ответ
```json
{
  "totalReceivables": 398200.00,
  "overdueReceivables": 358000.00,
  "overduePercentage": 89.9,
  "currentReceivables": 40200.00,
  "averagePaymentDelayDays": 67.5,
  "totalInvoicesCount": 25,
  "overdueInvoicesCount": 21,
  "agingStructure": [
    {"bucket": "Current", "amount": 40200.00, "count": 4},
    {"bucket": "1-30", "amount": 32500.00, "count": 2},
    {"bucket": "31-60", "amount": 34700.00, "count": 3},
    {"bucket": "61-90", "amount": 83500.00, "count": 3},
    {"bucket": "91+", "amount": 207300.00, "count": 13}
  ]
}
```

#### Метрики
- ✅ Общая ДЗ
- ✅ Просроченная ДЗ
- ✅ % просроченной ДЗ
- ✅ Непросроченная ДЗ
- ✅ Средний срок просрочки
- ✅ Количество счетов
- ✅ Aging структура (5 бакетов)

---

### 4. **Топ-10 Должников** ✓

#### Endpoint
```
GET /api/v1/reports/top-debtors?limit=10
Authorization: Bearer <token>
```

#### Ответ
```json
[
  {
    "customerId": "507f1f77bcf86cd799439011",
    "customerName": "ООО \"МетПром\"",
    "customerInn": "246813579",
    "totalDebt": 58000.00,
    "overdueDebt": 58000.00,
    "invoiceCount": 1,
    "oldestDebtDays": 226
  },
  ...
]
```

---

### 5. **Список Счетов с Фильтрацией** ✓

#### Endpoint
```
GET /api/v1/reports/invoices
Authorization: Bearer <token>
```

#### Параметры фильтрации
- `status` - статус счета (OPEN, PAID, OVERDUE)
- `debtWorkStatus` - статус работы с долгом (IN_TIME, CALL, CLAIM, PRE_TRIAL, TRIAL, COLLECTION, WRITE_OFF, CLOSED)
- `serviceType` - тип услуги
- `manager` - менеджер (частичное совпадение)
- `customerId` - ID клиента
- `isOverdue` - только просроченные (true/false)
- `minAmount` - минимальная сумма задолженности
- `maxAmount` - максимальная сумма задолженности
- `dueDateFrom` - срок оплаты от (ISO date)
- `dueDateTo` - срок оплаты до (ISO date)

#### Параметры пагинации
- `limit` - количество записей (по умолчанию 50)
- `offset` - смещение (по умолчанию 0)
- `sortBy` - поле сортировки (по умолчанию dueDate)
- `sortOrder` - порядок сортировки (asc/desc, по умолчанию asc)

#### Примеры запросов

**Просроченные счета с долгом > 10000:**
```
GET /api/v1/reports/invoices?isOverdue=true&minAmount=10000&sortBy=dueDate&sortOrder=desc
```

**Счета по менеджеру "Иванова" в статусе "Претензия":**
```
GET /api/v1/reports/invoices?manager=Иванова&debtWorkStatus=CLAIM
```

#### Ответ
```json
{
  "invoices": [
    {
      "id": "507f1f77bcf86cd799439011",
      "invoiceNumber": "АКТ-2024-0234",
      "customerId": "507f191e810c19729de860ea",
      "customer": {
        "id": "507f191e810c19729de860ea",
        "name": "ООО \"АльфаТорг\"",
        "inn": "123456789"
      },
      "issueDate": "2024-07-31T00:00:00.000Z",
      "dueDate": "2024-08-30T00:00:00.000Z",
      "serviceStartDate": "2024-07-01T00:00:00.000Z",
      "serviceEndDate": "2024-07-31T00:00:00.000Z",
      "totalAmount": 15000,
      "paidAmount": 0,
      "outstandingAmount": 15000,
      "paymentTermDays": 30,
      "actualPaymentDate": null,
      "status": "OVERDUE",
      "debtWorkStatus": "CLAIM",
      "serviceType": "PKT_SUPPORT",
      "manager": "Иванова М.П.",
      "lastContactDate": null,
      "contactResult": null,
      "notes": "Просрочка 42 дня",
      "createdAt": "2024-08-15T10:30:00.000Z",
      "updatedAt": "2024-08-15T10:30:00.000Z"
    }
  ],
  "total": 15,
  "limit": 50,
  "offset": 0
}
```

---

### 6. **Применение Частичной Оплаты** ✓

#### Endpoint
```
POST /api/v1/reports/invoices/apply-payment
Content-Type: application/json
Authorization: Bearer <token>
```

#### Запрос
```json
{
  "invoiceId": "507f1f77bcf86cd799439011",
  "amount": 5000.00,
  "paymentDate": "2024-11-11T00:00:00.000Z"  // опционально
}
```

#### Ответ
```json
{
  "id": "507f1f77bcf86cd799439011",
  "invoiceNumber": "АКТ-2024-0234",
  "totalAmount": 15000,
  "paidAmount": 5000,
  "outstandingAmount": 10000,
  "status": "OVERDUE",
  "actualPaymentDate": "2024-11-11T00:00:00.000Z",
  ...
}
```

#### Валидация
- ✅ Сумма оплаты > 0
- ✅ Сумма оплаты ≤ остаток задолженности
- ✅ Автоматическое обновление статуса при полной оплате

---

## 📈 Типы Услуг (ServiceType)

| Тип | Код | Описание |
|-----|-----|----------|
| Сопровождение ПКТ | `PKT_SUPPORT` | Сопровождение программных кассовых терминалов |
| Установка ККТ | `KKT_INSTALLATION` | Установка контрольно-кассовой техники |
| Обслуживание ККТ | `KKT_SERVICE` | Обслуживание ККТ |
| Обслуживание торг. автоматов | `VENDING_SERVICE` | Обслуживание торговых автоматов |
| Установка торг. автомата | `VENDING_INSTALLATION` | Установка торгового автомата |
| Прочее | `OTHER` | Другие виды услуг |

---

## 🚦 Статусы Работы с Долгом (DebtWorkStatus)

| Статус | Код | Просрочка (дни) | Действия |
|--------|-----|-----------------|----------|
| В срок | `IN_TIME` | 0 | Срок оплаты не наступил |
| Прозвон | `CALL` | 1-30 | Телефонное напоминание |
| Претензия | `CLAIM` | 31-60 | Направление претензии |
| Подготовка к суду | `PRE_TRIAL` | 61-90 | Сбор документов для суда |
| Суд | `TRIAL` | 91-180 | Судебное разбирательство |
| Взыскание | `COLLECTION` | 180+ | Исполнительное производство |
| Списание | `WRITE_OFF` | - | Безнадежный долг |
| Закрыт | `CLOSED` | - | Долг оплачен |

---

## 🔧 Технические Детали

### Архитектура
- **Clean Architecture** (Domain, Application, Infrastructure)
- **DI Container** (tsyringe)
- **MongoDB** с агрегационными пайплайнами
- **TypeScript** + **Express.js**

### Индексы MongoDB
```javascript
// Составной индекс для быстрого поиска
{ customerId: 1, invoiceNumber: 1 } // unique

// Индексы для фильтрации
{ manager: 1, debtWorkStatus: 1 }
{ status: 1 }
{ dueDate: 1 }
{ serviceType: 1 }
```

### Производительность
- Пагинация для больших выборок
- Параллельные запросы (count + find)
- Агрегационные пайплайны для аналитики
- Populate для связанных данных

---

## 📝 Примеры Использования

### Сценарий 1: Импорт данных из 1С
```bash
curl -X POST http://localhost:3000/api/v1/data-uploads/1c-invoices \
  -H "Authorization: Bearer <token>" \
  -F "file=@выгрузка1c.csv"
```

### Сценарий 2: Получение просроченных счетов требующих прозвона
```bash
curl -X GET "http://localhost:3000/api/v1/reports/invoices?debtWorkStatus=CALL&isOverdue=true" \
  -H "Authorization: Bearer <token>"
```

### Сценарий 3: Применение частичной оплаты
```bash
curl -X POST http://localhost:3000/api/v1/reports/invoices/apply-payment \
  -H "Authorization: Bearer <token>" \
  -H "Content-Type: application/json" \
  -d '{
    "invoiceId": "507f1f77bcf86cd799439011",
    "amount": 5000
  }'
```

### Сценарий 4: Топ-10 должников для работы юристов
```bash
curl -X GET "http://localhost:3000/api/v1/reports/top-debtors?limit=10" \
  -H "Authorization: Bearer <token>"
```

---

## 🎯 Дальнейшие Улучшения (для v2.0)

### Функциональные
- [ ] Автоматические уведомления менеджерам при просрочке
- [ ] История контактов с должниками
- [ ] Планирование действий по взысканию
- [ ] ABC-анализ контрагентов
- [ ] Прогнозирование оплат (ML)
- [ ] Экспорт отчетов в Excel/PDF
- [ ] Интеграция с email для отправки претензий
- [ ] Webhook для уведомлений об изменениях

### Технические
- [ ] Кэширование для дашборда (Redis)
- [ ] WebSocket для real-time обновлений
- [ ] Batch обработка больших выгрузок
- [ ] Оптимистичные блокировки для конкурентных обновлений
- [ ] Логирование всех действий (audit trail)
- [ ] Unit/Integration тесты для новых use-cases

---

## 📚 Swagger Документация

После запуска сервера:
```
http://localhost:3000/api-docs
```

Все эндпоинты документированы с примерами запросов и ответов.

---

## ⚡ Quick Start

```bash
# 1. Установка зависимостей
npm install

# 2. Настройка .env
cp .env.example .env
# Настройте MongoDB connection string

# 3. Запуск в режиме разработки
npm run dev

# 4. Импорт тестовых данных
curl -X POST http://localhost:3000/api/v1/auth/register \
  -H "Content-Type: application/json" \
  -d '{"email": "admin@test.com", "password": "password123", "name": "Admin"}'

# Получить токен
curl -X POST http://localhost:3000/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email": "admin@test.com", "password": "password123"}'

# Загрузить CSV
curl -X POST http://localhost:3000/api/v1/data-uploads/1c-invoices \
  -H "Authorization: Bearer <token>" \
  -F "file=@выгрузка1c.csv"

# Проверить дашборд
curl http://localhost:3000/api/v1/reports/dashboard/summary \
  -H "Authorization: Bearer <token>"
```

---

## 🤝 Поддержка

При возникновении вопросов или обнаружении багов, пожалуйста, создайте issue в репозитории.

**MVP реализован! Готов к тестированию! 🚀**

